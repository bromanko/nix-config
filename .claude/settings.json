{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for f in $CLAUDE_FILE_PATHS; do [[ \"$f\" == *.nix ]] && nix fmt -- \"$f\" 2>/dev/null; done; true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git status --porcelain -- '*.nix' | grep -q . && nix flake check || true"
          }
        ]
      }
    ]
  }
}
